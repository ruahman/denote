#+title:      dependency inversion
#+date:       [2023-06-20 Tue 19:10]
#+filetags:   :design-patterns:solid:tech:
#+identifier: 20230620T191014

you code should depend on abstractions rather than concrete implementations

you code should depend on interface rather than actual implementations

implementation does not matter nor is it your concern
